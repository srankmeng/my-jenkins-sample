pipeline {
    agent any

    stages {
        stage('Code analytic') {
            steps {
                echo 'Code analytic'
            }
        }
    }
    post {
        always {
            echo 'deploy done'
        }
    }
}
